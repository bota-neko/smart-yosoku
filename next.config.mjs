/**
 * Next.js 設定。
 * 現時点では App Router の標準設定のみ。Supabase 画像や環境変数追加は
 * 別担当が接続する際に拡張する想定。
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
